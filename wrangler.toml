name = "mizuki"
compatibility_date = "2026-05-16"
compatibility_flags = ["nodejs_compat"]

# If you already have a main entry produced by the Astro Cloudflare adapter,
# uncomment and adjust the following line to match your build output.
# main = "dist/worker.mjs"
